<%@include file="includes/setup.md.rsp"%>

<%@string fcnname="rowProds"%>
<% fcnname <- "<%@string name="fcnname"%>" %>
<%@meta title="${fcnname}() benchmarks"%>
<%@meta author="Henrik Bengtsson"%>
<%@meta date="2014-06-02"%>

<%@include file="${header}"%>


# <%@meta name="title"%>

This report benchmark the performance of <%=fcnname%>() against alternative methods.

## Alternative methods

* apply() + prod()

See also [StackOverflow:rowProds?].


## Data
```r
<%=withCapture({
<%@include file="R/random-matrices.R"%>
data <- rmatrices(mode="double")
})%>
```

## Results

<% for (ii in seq_along(data)) { %>
<%
dataLabel <- names(data)[ii]
X <- data[[dataLabel]]
gc()
%>
### <%=dataLabel%> (<%=paste(dim(X), collapse="-by-")%>) matrix

```r
<%=withCapture({
X <- data[[.dataLabel.]]
stats <- microbenchmark(
 rowProds = rowProds(X, na.rm=FALSE),
 apply    = apply(X, MARGIN=1L, FUN=prod, na.rm=FALSE),
 unit = "ms"
)
})%>
```

<% benchmarkResults(stats, tags=dataLabel) %>

<%
# Sanity checks
Y <- list(
 rowProds = rowProds(X, na.rm=FALSE),
 apply    = apply(X, MARGIN=1L, FUN=prod, na.rm=FALSE)
)
lapply(Y, FUN=function(x) stopifnot(isTRUE(all.equal(x, Y[[1]]))))
%>

<% } # for (ii ...) %>


<%@include file="${footer}"%>


<%---------------------------------------------------------------------------
HISTORY:
2014-06-02
o Created.
---------------------------------------------------------------------------%>
