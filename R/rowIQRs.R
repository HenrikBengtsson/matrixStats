rowIQRs <- function(x, ...) {
  rowQuantiles(x, probs=c(0.25, 0.75), ...);
}


colIQRs <- function(x, ...) {
  colQuantiles(x, probs=c(0.25, 0.75), ...);
}


############################################################################
# HISTORY:
# 2008-03-26 [HB] 
# o Created.
############################################################################
